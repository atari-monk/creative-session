export interface PlayerObjectOptions {
  id: string;
  radius: number;
  speed: number;
  width: number;
  height: number;
  keys: any; // Replace 'any' with the actual type of 'keys'
  color: any; // Replace 'any' with the actual type of 'color'
  isPlayable: boolean;
  playerNr: number;
}
